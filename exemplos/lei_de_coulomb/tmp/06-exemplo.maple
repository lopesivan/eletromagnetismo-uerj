restart;
with(Physics[Vectors]);
Setup(mathematicalnotation = true);
interface(displayprecision = 3);

F_ := 1/(4*Pi*varepsilon__0)*q__1*q__2*_r/Norm(r_)^2;

varepsilon__0 := 0.8854*10^(-11);

r_   := 2*_i + 6*_j;
q__1 := 0.300;
q__2 := 0.600;

evalf(F_);

# fim
