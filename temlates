interface(imaginaryunit = J); # usar J em lugar de I
polard := (mag, angd) -> evalc(polar(mag, 1/180*angd*Pi));

restart;
with(Student[LinearAlgebra]);
interface(showassumed = 0);  # desabilita var~



reset;
restart;
with(LinearAlgebra);
assume(0 < t);
interface(showassumed = 0);
t;
