restart:
with(Student[LinearAlgebra]):

# Lista de componentes
A := [2,3,4];
B := [3,5,9];
C := [2,1,0];
X := [x,y,z];

AB := B - A;
AC := C - A;

u_ := < op(AB) >;
v_ := < op(AC) >;

u_ . u_;
u_ &x v_;

# fim
