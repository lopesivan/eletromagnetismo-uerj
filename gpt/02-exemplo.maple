restart;
with(Student[LinearAlgebra]);

r_ := <2, 6>;

varepsilon__0 := 0.8854*10^(-11);

q__1 := 0.300;
q__2 := 0.600;

norma_r  := Norm(r_, 2);
versor_r := r_/norma_r;

F_ := 1/(4*Pi*varepsilon__0) * q__1*q__2 * versor_r/norma_r^2;

evalf(F_);

F := Norm(F_, 2);

# fim
